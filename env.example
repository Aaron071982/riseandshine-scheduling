# =============================================================================
# SCHEDULING AI ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in your values
# =============================================================================

# -----------------------------------------------------------------------------
# SCHEDULING DATABASE (Separate from HRM!)
# -----------------------------------------------------------------------------
# Create a NEW Supabase project specifically for Scheduling AI
# This isolates scheduling data from HRM to prevent accidental changes
# -----------------------------------------------------------------------------
SUPABASE_SCHED_URL=https://your-scheduling-project.supabase.co
SUPABASE_SCHED_ANON_KEY=your-scheduling-anon-key
SUPABASE_SCHED_SERVICE_ROLE_KEY=your-scheduling-service-role-key

# Project reference (extracted from URL) - used for safety validation
# Example: if URL is https://abcdefghij.supabase.co, PROJECT_REF is "abcdefghij"
SUPABASE_SCHED_PROJECT_REF=your-scheduling-project-ref

# -----------------------------------------------------------------------------
# API SERVER
# -----------------------------------------------------------------------------
# Port for the secure API server (handles manual pin updates, etc.)
API_PORT=3001

# CORS origin for API requests (your frontend URL)
CORS_ORIGIN=http://localhost:3000

# -----------------------------------------------------------------------------
# GOOGLE MAPS API
# -----------------------------------------------------------------------------
# Required for geocoding and travel time calculations
# Enable: Geocoding API, Distance Matrix API, Maps JavaScript API
# -----------------------------------------------------------------------------
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# -----------------------------------------------------------------------------
# TRAVEL TIME CONFIGURATION
# -----------------------------------------------------------------------------
# Traffic model for travel time estimates
# Options: best_guess, pessimistic, optimistic
# Default: pessimistic (recommended for scheduling to avoid being late)
TRAFFIC_MODEL=pessimistic

# How long to cache travel time calculations (in days)
# Default: 14 days
TRAVEL_TIME_TTL_DAYS=14

# -----------------------------------------------------------------------------
# CRM DATABASE (READ-ONLY - For client data)
# -----------------------------------------------------------------------------
# Separate Supabase project for CRM/client data
# Clients are synced from CRM to Scheduling DB via sync job
# -----------------------------------------------------------------------------
CRM_SUPABASE_URL=https://your-crm-project.supabase.co
CRM_SUPABASE_SERVICE_ROLE_KEY=your-crm-service-role-key
# Optional: Use anon key if service role not available (read-only access)
# CRM_SUPABASE_ANON_KEY=your-crm-anon-key

# -----------------------------------------------------------------------------
# LEGACY HRM DATABASE (READ-ONLY - Do not use for scheduling writes!)
# -----------------------------------------------------------------------------
# These are for reading RBT data from the existing HRM system
# DO NOT write to this database from the scheduling system
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_SUPABASE_URL=https://your-hrm-project.supabase.co
# SUPABASE_URL=https://your-hrm-project.supabase.co
# SUPABASE_ANON_KEY=your-hrm-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-hrm-service-role-key

# -----------------------------------------------------------------------------
# OPTIONAL: HRM DATA SYNC
# -----------------------------------------------------------------------------
# If you need to sync RBT data from HRM to Scheduling DB
# Set this to 'true' to enable the sync feature
# HRM_SYNC_ENABLED=false
# HRM_SYNC_INTERVAL_HOURS=24

